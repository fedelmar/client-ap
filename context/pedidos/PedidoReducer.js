import {
    SELECCIONAR_CLIENTE,
    SELECCIONAR_PRODUCTO,
    CANTIDAD_PRODUCTO
} from '../../types'


export default ( state, accion ) => {
    switch(accion.type) {


        default:
            return state
    }
}